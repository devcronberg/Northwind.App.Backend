# Environment Variables Template
# Copy this file to .env and fill in your local values
# DO NOT commit .env to source control!

# ASP.NET Core Runtime Settings
# These are standard ASP.NET Core environment variables (not from appsettings.json)
ASPNETCORE_ENVIRONMENT=Development
ASPNETCORE_URLS=http://localhost:5033

# JWT Configuration (REQUIRED - override appsettings.json default)
# Secret must be at least 32 characters long for HS256 algorithm
Jwt__Secret=your-super-secret-jwt-key-change-this-min-32-chars-long!

# JWT Configuration (Optional overrides - defaults in appsettings.json)
# Jwt__Issuer=Northwind.App.Backend
# Jwt__Audience=Northwind.App.Frontend
# Jwt__AccessTokenExpirationMinutes=60
# Jwt__RefreshTokenExpirationDays=7

# Database Connection String (Optional override - default is SQLite in Assets folder)
# Uncomment and modify only if you need a different database
# ConnectionStrings__NorthwindDb=Data Source=Assets/Northwind.db
# For SQL Server (example):
# ConnectionStrings__NorthwindDb=Server=localhost;Database=Northwind;User Id=sa;Password=YourPassword;TrustServerCertificate=True

# Logging Configuration (Optional overrides)
# Logging__LogLevel__Default=Information
# Logging__LogLevel__Microsoft.AspNetCore=Warning

# OpenRouter AI Configuration
OpenRouter__Url=https://openrouter.ai/api/v1/chat/completions
OpenRouter__ApiKey=sk-or-v1-your-api-key-here
OpenRouter__Model=anthropic/claude-sonnet-4.5

# Notes:
# - ASPNETCORE_* are runtime settings for the ASP.NET Core host
# - Jwt__Secret is the ONLY required override (safe default exists but should be changed)
# - Other settings have safe defaults in appsettings.json
# - Use double underscore (__) to represent nested JSON sections
# - Example: Jwt__Secret maps to { "Jwt": { "Secret": "..." } } in appsettings.json
# - In production (Docker/Render), use environment variables instead of .env files
